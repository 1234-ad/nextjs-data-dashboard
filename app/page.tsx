import { EmployeeDashboard } from '@/components/EmployeeDashboard';

/**
 * Home page component that renders the main employee dashboard
 */
export default function HomePage() {
  return <EmployeeDashboard />;
}